;; this is a test right here
(define (neg n) ;; all of these lines will be ignored
    (not (x)))  ;; see hopefully they will |(hope this dont work)
; this function right here returns the inverse of X
    